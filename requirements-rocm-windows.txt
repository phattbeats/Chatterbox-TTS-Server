# requirements-rocm-windows.txt — AMD Windows fork
# Windows ROCm support for AMD Radeon RX 6000 series (gfx1030/1031/1032)
#
# NOTE: This file is a reference manifest only. The normal install path is
#   python start.py --rocm-windows
# which downloads the ROCm/torch wheels directly from the GitHub release
# (tag v0.1.0-rocm-wheels) and installs them from local disk via
# `pip install --no-index --find-links`. You do not need a pip index — these
# wheels are NOT on PyPI and are gfx103X / Python 3.12 (cp312) only.
# See README.md for the full install walkthrough.

# ROCm SDK meta-packages (built from guinmoon's TheRock-based rebuild)
rocm==7.1.1
rocm-sdk-core==7.1.1
rocm-sdk-devel==7.1.1
rocm-sdk-libraries-gfx103x-all==7.1.1

# PyTorch ROCm wheels (Python 3.12, Windows x86_64)
torch==2.9.1+rocmsdk20251207
torchaudio==2.9.0+rocmsdk20251207
torchvision==0.24.0+rocmsdk20251207

# Chatterbox TTS dependencies (mirrors requirements-rocm.txt, no torch pins)
conformer==0.3.2
diffusers==0.29.0
transformers==4.46.3
einops>=0.7.0
omegaconf==2.3.0
resampy==0.4.3
resemble-perth==1.0.1
fastapi>=0.100.0,<0.116.0
uvicorn[standard]>=0.27.0
numpy>=1.24.0
soundfile>=0.12.0
huggingface_hub>=0.20.0
descript-audio-codec==1.0.0
safetensors>=0.4.0
pydantic>=2.0.0
python-dotenv>=1.0.0
Jinja2>=3.1.2,<3.2.0
python-multipart>=0.0.5
requests>=2.28.0
PyYAML>=6.0
tqdm>=4.65.0
pydub>=0.25.0
praat-parselmouth>=0.4.0
librosa>=0.10.0
hf-transfer>=0.1.4
